sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t docker/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true docker/docker-react npm run test -- --coverage
